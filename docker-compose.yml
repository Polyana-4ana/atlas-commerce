services:
  postgres:
    image: postgres:17
    container_name: atlas-postgres

    environment:
      POSTGRES_DB: atlasdb
      POSTGRES_USER: atlas
      POSTGRES_PASSWORD: atlas123

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: